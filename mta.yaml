_schema-version: 3.3.0
ID: AIPoweredSkillGapAnalysis
description: A simple CAP project.
version: 1.0.0
modules:
- name: AIPoweredSkillGapAnalysis-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: AIPoweredSkillGapAnalysis-auth
  - name: AIPoweredSkillGapAnalysis-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: AIPoweredSkillGapAnalysis-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: AIPoweredSkillGapAnalysis-db
  parameters:
    buildpack: nodejs_buildpack
- name: AIPoweredSkillGapAnalysis-destination-content
  type: com.sap.application.content
  requires:
  - name: AIPoweredSkillGapAnalysis-destination-service
    parameters:
      content-target: true
  - name: AIPoweredSkillGapAnalysis_html_repo_host
    parameters:
      service-key:
        name: AIPoweredSkillGapAnalysis_html_repo_host-key
  - name: AIPoweredSkillGapAnalysis-auth
    parameters:
      service-key:
        name: AIPoweredSkillGapAnalysis-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: listReportPage_AIPoweredSkillGapAnalysis_html_repo_host
          ServiceInstanceName: AIPoweredSkillGapAnalysis-html5-app-host-service
          ServiceKeyName: AIPoweredSkillGapAnalysis_html_repo_host-key
          sap.cloud.service: listReportPage
        - Authentication: OAuth2UserTokenExchange
          Name: listReportPage_AIPoweredSkillGapAnalysis_auth
          ServiceInstanceName: AIPoweredSkillGapAnalysis-auth
          ServiceKeyName: AIPoweredSkillGapAnalysis-auth-key
          sap.cloud.service: listReportPage
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: AIPoweredSkillGapAnalysis-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: AIPoweredSkillGapAnalysis_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comcandyappskillgapanalysis.zip
      name: comcandyappskillgapanalysis
      target-path: resources/
- name: comcandyappskillgapanalysis
  type: html5
  path: app/skillgapanalysis
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: AIPoweredSkillGapAnalysis-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: AIPoweredSkillGapAnalysis-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: AIPoweredSkillGapAnalysis-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: AIPoweredSkillGapAnalysis-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: AIPoweredSkillGapAnalysis-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: AIPoweredSkillGapAnalysis_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: AIPoweredSkillGapAnalysis-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
